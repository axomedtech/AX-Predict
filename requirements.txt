# Step 1
mysql -u root -p < db/setup.sql
mysql -u root -p < db/setup.sql   # run inserts too

# Step 2
pip install -r requirements.txt

# Step 3
python src/train_model.py

# Step 4
python src/predict.py